let order = new Promise(function (resolve, reject) {
    let foodready = true;
    if (feedready) {
        reslove("pizza is delivered");

    } else {
        reject("order Cancelled")
    }

});

console.log(order);

